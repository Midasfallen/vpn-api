SECRET_KEY=replace-this-with-a-random-secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
# PROMOTE_SECRET is used to bootstrap/admin promotion in the app. Keep it secret.
PROMOTE_SECRET=replace-with-bootstrap-secret
# Example DATABASE_URL for PostgreSQL (SQLAlchemy / Alembic):
#   postgresql+psycopg2://DB_USER:DB_PASSWORD@DB_HOST:DB_PORT/DB_NAME
# If your password contains special characters, URL-encode it (percent-encoding).
# Example with values:
#   postgresql+psycopg2://midas:pa%40ssword@146.103.99.70:5432/vpn
DATABASE_URL=postgresql+psycopg2://DB_USER:DB_PASSWORD@DB_HOST:DB_PORT/DB_NAME
