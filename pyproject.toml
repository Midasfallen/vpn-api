[tool.black]
line-length = 88
target-version = ['py311']

[tool.ruff]
line-length = 88
exclude = ["alembic/*", "artifacts/*", "ci-artifacts/*", "venv/*", ".git/*"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "C", "D", "RUF", "B"]

[tool.isort]
profile = "black"
