# Example production env file. Copy to .env.production on the server and fill with real secrets.
DATABASE_URL=postgresql+psycopg2://DB_USER:DB_PASSWORD@DB_HOST:5432/vpn
UVICORN_WORKERS=2
SECRET_KEY=replace-me
